---
import BaseLayout from "../layouts/BaseLayout.astro";
import BeforeAfter from "../components/BeforeAfter.jsx";
import FaqAccordion from "../components/FaqAccordion.jsx";
import Sample from "../components/Sample.astro";
import site, { waLink } from "../data/site.js";
import { professionalService, faqPage } from "../lib/schema.js";

const homeSamples = site.samples.slice(0, 3);
const jsonLd = [professionalService(), faqPage(site.faqs)];
const sampleMsg =
  "Hi Harinda, I found your site. Could you do a free sample edit of a few of my photos?";
const orderMsg =
  "Hi Harinda, I found you through your site and I'd like a quote for photo editing.";
const { rating, reviews } = site.fiverrStats;
---

<BaseLayout
  title="Human photo editing and colour correction"
  description={site.description}
  path="/"
  jsonLd={jsonLd}
>
  <!-- ── Hero ─────────────────────────────────────────────────────────── -->
  <section class="hero">
    <div class="container hero__text">
      <h1>Colour correction and retouching, done by a human eye.</h1>
      <p>
        {site.tagline} No AI presets, no one-click batch looks. Skin stays skin,
        whites stay white — and you can see it first with a free sample edit.
      </p>
      <div class="btn-row">
        <a class="btn" href={waLink(sampleMsg)}>{site.freeSample.cta}</a>
        <a class="btn btn--ghost" href="/pricing/">See pricing</a>
      </div>
      <p class="hero__trust">
        Rated {rating} on <a href={site.contact.fiverr} target="_blank" rel="noopener">Fiverr</a>
        over {reviews} reviews &middot; {site.areasServed.join(" &middot; ")}
      </p>
    </div>

    <figure class="hero__figure">
      <BeforeAfter
        client:load
        variant="hero"
        sweep="load"
        eager
        priority
        before={site.hero.before}
        after={site.hero.after}
        width={site.hero.width}
        height={site.hero.height}
      />
      <figcaption>Drag the handle to compare the original file with the finished edit.</figcaption>
    </figure>
  </section>

  <!-- ── What you get ─────────────────────────────────────────────────── -->
  <section class="section">
    <div class="container">
      <div class="section-head">
        <span class="eyebrow">What you get</span>
        <h2 class="h2">Editing decisions made by a person, not a model.</h2>
        <p>Every frame is opened on a calibrated screen and judged by eye. Here is what that changes.</p>
      </div>
      <div class="grid">
        {
          site.whatYouGet.map((f) => (
            <div class="feature">
              <h3>{f.title}</h3>
              <p>{f.body}</p>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- ── Sample before/afters ─────────────────────────────────────────── -->
  <section class="section section--dim">
    <div class="container">
      <div class="section-head">
        <span class="eyebrow">Sample work</span>
        <h2 class="h2">A few before and afters.</h2>
        <p>Every one is a live slider — drag to compare the RAW file with the finished edit.</p>
      </div>
      <div class="samples">
        {homeSamples.map((s) => <Sample sample={s} />)}
      </div>
      <p style="margin-top:2.5rem">
        <a class="text-link" href="/work/">See more work</a>
      </p>
    </div>
  </section>

  <!-- ── Free sample callout ─────────────────────────────────────────── -->
  <section class="section">
    <div class="container">
      <div class="callout">
        <div>
          <h2>{site.freeSample.short}</h2>
          <p>{site.freeSample.line} No card, no commitment.</p>
        </div>
        <a class="btn" href={waLink(sampleMsg)}>{site.freeSample.cta}</a>
      </div>
    </div>
  </section>

  <!-- ── Pricing snapshot ─────────────────────────────────────────────── -->
  <section class="section section--dim">
    <div class="container">
      <div class="section-head">
        <span class="eyebrow">Pricing</span>
        <h2 class="h2">Priced by volume. Paid after delivery.</h2>
        <p>Three flat packages. Larger sets are quoted individually.</p>
      </div>
      <div class="price-grid">
        {
          site.packages.map((p) => (
            <div class="price-card">
              <div class="price-card__photos">{p.photos} photos</div>
              <div class="price-card__amount">${p.price}</div>
              <div class="price-card__unit">
                ${(p.price / p.photos).toFixed(2)} per photo, all in
              </div>
            </div>
          ))
        }
      </div>
      <p class="bulk-note">{site.bulkNote}</p>
      <p style="margin-top:1.5rem">
        <a class="text-link" href="/pricing/">Full pricing and payment methods</a>
      </p>
    </div>
  </section>

  <!-- ── How it works (numbered) ─────────────────────────────────────── -->
  <section class="section section--band">
    <div class="container">
      <div class="section-head">
        <span class="eyebrow">How it works</span>
        <h2 class="h2">Four steps, no deposit.</h2>
      </div>
      <ol class="steps">
        {
          site.howItWorks.map((step) => (
            <li class="step">
              <span class="step__n" aria-hidden="true" />
              <h3>{step.title}</h3>
              <p>{step.body}</p>
            </li>
          ))
        }
      </ol>
    </div>
  </section>

  <!-- ── Testimonials ────────────────────────────────────────────────── -->
  <section class="section">
    <div class="container">
      <div class="section-head">
        <span class="eyebrow">In their words</span>
        <h2 class="h2">What clients say.</h2>
        <p>
          {rating} out of 5 across {reviews} reviews on Fiverr. TODO — replace the
          quotes below with verbatim reviews and reviewer names.
        </p>
      </div>
      <div class="quotes">
        {
          site.testimonials.map((t) => (
            <figure class="quote">
              <blockquote>&ldquo;{t.quote}&rdquo;</blockquote>
              <figcaption>
                <b>{t.name}</b>
                <br />
                {t.role}
              </figcaption>
            </figure>
          ))
        }
      </div>
    </div>
  </section>

  <!-- ── FAQ ─────────────────────────────────────────────────────────── -->
  <section class="section section--dim">
    <div class="container">
      <div class="section-head">
        <span class="eyebrow">Questions</span>
        <h2 class="h2">Frequently asked.</h2>
      </div>
      <FaqAccordion client:visible items={site.faqs} />
    </div>
  </section>

  <!-- ── Contact CTA ─────────────────────────────────────────────────── -->
  <section class="section section--band">
    <div class="container center-narrow">
      <h2 class="h2">Start with a free sample edit.</h2>
      <p style="margin-top:.75rem;color:var(--on-band-soft)">
        Send two or three RAW files. You get them back fully edited, free, and you
        decide from there. Full orders are paid only after delivery.
      </p>
      <div class="btn-row" style="margin-top:1.5rem">
        <a class="btn" href={waLink(sampleMsg)}>Message on WhatsApp</a>
        <a class="btn btn--ghost" href={site.contact.fiverr} target="_blank" rel="noopener">Book on Fiverr</a>
      </div>
    </div>
  </section>
</BaseLayout>
